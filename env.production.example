# ============================================
# AGRO365 - CONFIGURACIÓN DE PRODUCCIÓN
# ============================================
# 
# INSTRUCCIONES:
# 1. Copia este archivo como .env en el servidor de producción
# 2. Genera una nueva APP_KEY: php artisan key:generate
# 3. Completa todas las variables con tus valores reales
# 4. NUNCA subas el archivo .env al repositorio
#
# ============================================

# ============================================
# APLICACIÓN
# ============================================
APP_NAME="Agro365"
APP_ENV=production
APP_KEY=  # ⚠️ Generar con: php artisan key:generate
APP_DEBUG=false
APP_TIMEZONE=Europe/Madrid  # ⚠️ Zona horaria de España
APP_URL=https://tudominio.com  # ⚠️ Cambiar por tu dominio real
APP_LOCALE=es
APP_FALLBACK_LOCALE=es
APP_FAKER_LOCALE=es_ES

# ============================================
# BASE DE DATOS - MySQL/MariaDB (Hostinger)
# ============================================
DB_CONNECTION=mysql
DB_HOST=localhost  # En Hostinger normalmente es localhost
DB_PORT=3306
DB_DATABASE=agro365  # ⚠️ Nombre de tu base de datos
DB_USERNAME=agro365_user  # ⚠️ Usuario de BD (no root)
DB_PASSWORD=  # ⚠️ Cambiar por tu contraseña segura

# ============================================
# SESIONES
# ============================================
SESSION_DRIVER=database
SESSION_LIFETIME=180
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=  # Dejar vacío o tu dominio (ej: .tudominio.com)
SESSION_SECURE_COOKIE=true  # ⚠️ OBLIGATORIO en producción (requiere HTTPS)
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax
SESSION_PARTITIONED_COOKIE=false

# ============================================
# CACHE
# ============================================
CACHE_STORE=database
CACHE_PREFIX=agro365

# ============================================
# QUEUE
# ============================================
QUEUE_CONNECTION=database
DB_QUEUE_TABLE=jobs
DB_QUEUE_CONNECTION=

# ============================================
# EMAIL - Hostinger SMTP (Producción)
# ============================================
MAIL_MAILER=smtp
MAIL_HOST=smtp.hostinger.com
MAIL_PORT=465
MAIL_USERNAME=noreply@tudominio.com  # ⚠️ Cambiar por tu email
MAIL_PASSWORD=  # ⚠️ Cambiar por tu contraseña de email
MAIL_ENCRYPTION=ssl
MAIL_FROM_ADDRESS=noreply@tudominio.com  # ⚠️ Cambiar por tu email
MAIL_FROM_NAME="${APP_NAME}"

# ============================================
# LOGGING
# ============================================
LOG_CHANNEL=stack
LOG_STACK=daily
LOG_LEVEL=error
LOG_DAILY_DAYS=30
LOG_DEPRECATIONS_CHANNEL=null
LOG_DEPRECATIONS_TRACE=false

# ============================================
# BROADCASTING (Opcional)
# ============================================
BROADCAST_CONNECTION=log

# ============================================
# FILESYSTEM
# ============================================
FILESYSTEM_DISK=local

# ============================================
# AWS S3 (Opcional - para almacenamiento en la nube)
# ============================================
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=
# AWS_USE_PATH_STYLE_ENDPOINT=false

# ============================================
# REDIS (Opcional - para mejor rendimiento)
# ============================================
# REDIS_CLIENT=phpredis
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# ============================================
# SERVICIOS EXTERNOS (Opcional)
# ============================================
# PUSHER_APP_ID=
# PUSHER_APP_KEY=
# PUSHER_APP_SECRET=
# PUSHER_HOST=
# PUSHER_PORT=443
# PUSHER_SCHEME=https
# PUSHER_APP_CLUSTER=mt1

# ============================================
# NOTAS IMPORTANTES
# ============================================
#
# 1. SEGURIDAD:
#    - APP_DEBUG debe ser false en producción
#    - SESSION_SECURE_COOKIE debe ser true (requiere HTTPS)
#    - APP_KEY debe ser única y segura
#    - DB_PASSWORD debe ser fuerte
#
# 2. EMAIL:
#    - Configura Hostinger SMTP con tus credenciales
#    - Verifica que el dominio esté configurado en Hostinger
#
# 3. BASE DE DATOS:
#    - Usa un usuario dedicado (no postgres)
#    - Contraseña fuerte
#    - Backup automático configurado
#
# 4. HTTPS:
#    - OBLIGATORIO en producción
#    - Configura certificado SSL
#    - Redirige HTTP a HTTPS
#
# 5. SCHEDULER:
#    - Configura cron: * * * * * cd /ruta/proyecto && php artisan schedule:run
#    - O Task Scheduler en Windows
#
# 6. PERMISOS:
#    - storage/ y bootstrap/cache/ deben ser escribibles
#    - chmod -R 775 storage bootstrap/cache
#
# ============================================

